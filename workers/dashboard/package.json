{
  "name": "@fluxtube/dashboard",
  "version": "1.1.1",
  "private": true,
  "description": "Hono API worker for the FluxTube configuration dashboard.",
  "license": "MIT",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev --remote",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "lint": "eslint 'src/**/*.ts' 'test/**/*.ts'"
  },
  "dependencies": {
    "@simplewebauthn/server": "^13.3.2",
    "hono": "^4.12.34",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.22.0",
    "@cloudflare/workers-types": "^5.20260901.1",
    "typescript": "^6.0.3",
    "vite": "^8.2.2",
    "vitest": "~4.1.11",
    "wrangler": "^4.128.0"
  }
}
